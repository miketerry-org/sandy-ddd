db_driver=sqlite

# SQLite uses a file instead of host/port,
# but we include these for test compatibility
db_host=localhost
db_port=0
db_username=sqlite
db_password=sqlite

# Database file path
db_name=./test/test.sqlite

# Optional app paths (your original values)
public_path=public
views_path=views
