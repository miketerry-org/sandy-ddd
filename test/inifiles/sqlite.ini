## configuration values for postgres driver

# folder contains css, img, and js folders for static assets
public_path=public

# contains folderss and files for rendering html templates
views_path=views

#which database driver to use
db_driver=sqlite

# name of file to store sqlite database in
#db_filename=data.sqlite
